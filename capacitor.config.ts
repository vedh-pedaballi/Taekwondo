import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tornadosportsclub.app',
  appName: 'Tornado-Sports-Club',
  webDir: 'tornado-sports-app'
};

export default config;
